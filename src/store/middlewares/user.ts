const initState = {};

type initStateType = {};

export const userReducer = (
  state: initStateType = initState,
  action: any,
): initStateType => {
  switch (action.type) {
    case '': {
      return state;
    }
    default:
      return state;
  }
};
