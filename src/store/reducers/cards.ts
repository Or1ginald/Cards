const initState = {};

type initStateType = {};

export const cardReducer = (
    state: initStateType = initState,
    action: any,
): initStateType => {
    switch (action.type) {
        case '': {
            return state;
        }
        default:
            return state;
    }
};
